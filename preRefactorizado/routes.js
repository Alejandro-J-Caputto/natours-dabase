// const { getAllTours, getTourByID, postTour, deleteTour, editTour, patchTour } = require('./crudTours')
// const { getUser, createUser, editUser, deleteUser } = require('./users.crud')
// const express = require('express');

// const app = express();
// RUTAS Tours

// const tourRouter = express.Router();
// const userRouter = express.Router();




// tourRouter.route('/').get(getAllTours).post(postTour)
// tourRouter.route('/:id').get(getTourByID).put(editTour).delete(deleteTour).patch(patchTour)
//RUTAS USUARIOS


// userRouter.route('').get(getUser).post(createUser);

// userRouter.route('/:id').patch(editUser).delete(deleteUser);

// app.use('/api/v1/tours', tourRouter)
// app.use('/api/v1/users', userRouter)
// module.exports = app;